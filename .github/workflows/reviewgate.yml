name: ReviewGate

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write
  checks: write

jobs:
  reviewgate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Skip ReviewGate when OpenRouter is not configured
        if: ${{ env.OPENROUTER_API_KEY == '' }}
        run: echo "OPENROUTER_API_KEY is not configured; skipping ReviewGate."

      - name: ReviewGate
        if: ${{ env.OPENROUTER_API_KEY != '' }}
        uses: LVTD-LLC/reviewgate@v0
        with:
          preset: balanced
          gate_mode: report
    env:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
